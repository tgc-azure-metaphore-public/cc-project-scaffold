# <PROJECT_NAME> — environment variables
# Copy this file to .env and fill in real values.
# NEVER commit .env to git.

# === Common ===
# ANTHROPIC_API_KEY=       # required for any LLM-backed archetype (C, D, some B/E)

# === Archetype-specific env vars (generated based on detected archetype) ===
# <ARCHETYPE_SPECIFIC_ENV_VARS>

# === Deploy-specific ===
# PORT=8000                # Render, Fly, local dev
# DATABASE_URL=            # B archetype
# AUTH_SECRET=             # B archetype, NextAuth
# AUTH_URL=                # B archetype, NextAuth
